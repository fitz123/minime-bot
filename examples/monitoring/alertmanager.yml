route:
  receiver: minime-native-webhook
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: minime-native-webhook
    webhook_configs:
      - url: http://host.docker.internal:WEBHOOK_PORT/WEBHOOK_PATH_PLACEHOLDER
        send_resolved: true
